<h1>Tweet result for <%= search %></h1>
<% if(results.length) {%>
    <ul>
        <% for (let i = 0; i < results.length; i++) { %>
        <li><%= results[i].text %> - <%= results[i].from_user %></li>
        <% } %>
    </ul>
<% } else { %>
    <p>No results</p>
<% } %>


